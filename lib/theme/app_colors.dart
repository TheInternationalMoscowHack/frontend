import 'package:flutter/material.dart';

class AppColors {
  // Figma colors
  static const redMain = Color(0xFFEA221D);
  static const bigWhite = Color(0xFFFFFFFF);

  // Often used colors
  static const black = Color(0xFF000000);
  static const lightGray = Color(0xFFC4C4C4);

  const AppColors._();
}
